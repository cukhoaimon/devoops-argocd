apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: iceberg-catalog
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/your-repo  # git repo của bạn
    targetRevision: HEAD
    path: iceberg
  destination:
    server: https://kubernetes.default.svc
    namespace: data-warehouse
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true